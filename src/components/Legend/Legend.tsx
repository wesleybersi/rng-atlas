import styles from "./styles.module.scss";

const Legend = () => {
  return (
    <div className={styles.wrapper}>
      <button className={styles.option}>Show Countries</button>
    </div>
  );
};

export default Legend;
